/**
 * Cookie support module.
 * Provides support for parsing and setting cookies.
 * Cookie helpers (getCookie, setCookie) are available on the request context directly.
 */

// Cookie support is built-in via hono/cookie helpers (getCookie, setCookie).
// No middleware registration needed — helpers are available on Context directly.

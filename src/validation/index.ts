import { 
  validatorCompiler, 
  serializerCompiler, 
  ZodTypeProvider,
  jsonSchemaTransform,
  createJsonSchemaTransformObject
} from 'fastify-type-provider-zod';

// Export all the necessary components
export { 
  ZodTypeProvider,
  validatorCompiler,
  serializerCompiler,
  jsonSchemaTransform,
  createJsonSchemaTransformObject
};